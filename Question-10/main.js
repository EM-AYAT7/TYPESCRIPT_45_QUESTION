// Store the names of your friends in an array called names
var names = ["Ayat", "areeba", "zoya", "zara"];
// Print each person's name by accessing each element in the list, one at a time
names.forEach(function (name) {
    console.log(name);
});
