// Store the names of your friends in an array called names
var names11 = ["ayat", "zoya", "zahra", "hoorain"];
// Define the message to be personalized
var message = "Hello, ";
// Print a personalized message to each person
names11.forEach(function (name) {
    console.log(message + name + "!");
});
